# Kaggle and Arena portal comparison

Reviewed 12 September 2026 against Arena's current code and Kaggle's official documentation. Arena supports the core local learning and evaluation loop, but does not yet provide full Kaggle parity.

## Workflow comparison

| Area                     | Kaggle workflow                                                                 | Arena status and remaining work                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------ | ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Discovery and navigation | Browse resources, then open their detail pages and related work.                | Competitions, code, discussions and benchmark collections already have page routes. This update adds dataset/model pages with direct links, file details, previews where supported, and notebook creation. Learning and legacy CSV benchmark details still use older views.                                                                                                                     |
| Personal workspace       | Your Work brings created/shared resources and activity together.                | Owner management supports datasets, models, notebooks, competitions and benchmarks. This update preserves the selected category in the URL, including the benchmark landing page shortcut. A unified shared-with-me, bookmarks, activity and collections workspace remains incomplete.                                                                                                          |
| Datasets and reuse       | Start a notebook from a dataset or attach it from the editor.                   | This update attaches the selected dataset automatically, including its original CSV and pinned supplemental file versions. Existing editor input selection also supports competitions and notebook outputs. Dataset ingestion remains CSV-first and artifact uploads have a 10 MB per-file limit.                                                                                               |
| Models                   | Discover versioned models and use their hosted files in notebooks.              | This update gives models a detail page and attaches hosted model files to a new notebook. External reference-only cards explain why attachment is unavailable. Full model variants, large weight transfers, inference services and model-specific discussions remain incomplete. Model cards/files currently have public read access; dataset privacy should not be assumed to apply to models. |
| Notebook lifecycle       | Create/fork, edit, save versions, run and share.                                | Native editor, Markdown, imports, outputs, forks, version labels, sharing and CPU kernels exist. Resource-created drafts now retain their source context on the server. General background notebook commits independent of competition scoring, scheduling, interactive widgets and concurrent editing remain gaps.                                                                             |
| Competitions             | Join, inspect rules/data, develop code, submit predictions and compare results. | Arena has competition pages, membership, teams, inputs, submissions, discussions and local scoring. Competition publication is tied to successful evaluation. Organizer review tools, richer prize/timeline management, final/private leaderboard policies and leakage controls need further work. Official Titanic scoring remains unavailable because private answers were not imported.      |
| Discussions              | Topics contain comments and replies.                                            | Competition-scoped topics, an aggregated discovery page, pinned topics, filtering, bookmarks, comments/replies and rich text are implemented. Moderation operations and notification delivery need expansion.                                                                                                                                                                                   |
| Benchmarks               | Reusable tasks evaluate models and are combined into benchmark results.         | Arena supports versioned Python tasks, local models/configured API providers, isolated CPU runs, results and aggregate leaderboards. This is Arena's implementation, not full Kaggle SDK compatibility. Provider credentials/configuration are required for live API evaluation.                                                                                                                |
| Accounts and operations  | Profiles, sharing and resource management support ongoing work.                 | Profiles, avatars, groups, API tokens and settings exist. Local persistent storage and isolated workers support testing. GPU infrastructure, production identity integration, operational monitoring, quotas and backup/restore drills remain deployment work.                                                                                                                                  |

Kaggle references: [notebooks](https://www.kaggle.com/docs/notebooks), [datasets](https://www.kaggle.com/docs/datasets), [models](https://www.kaggle.com/docs/models), [Your Work announcement](https://www.kaggle.com/product-announcements/476860), and [benchmarks](https://www.kaggle.com/docs/benchmarks). Arena status is based on the repository implementation; it does not imply each feature has been tested at production scale.

## Changes in this update

- `#datasets/<id>` and `#models/<id>` open full detail pages from discovery and Your Work. Browser refresh and direct links retain the resource.
- **New notebook** creates a private temporary draft with the selected resource. File references are pinned when the draft is created, permission-checked again when opened, then materialized under its workspace `input/<source-folder>/` directory.
- Saving preserves the input metadata with the notebook. Discarding removes the temporary draft and its context; it does not delete the original dataset or model.
- A new additive `notebook_draft_inputs` table stores draft source context. Existing competition context and join requirements remain in place.
- Your Work categories use `#work/datasets`, `#work/models`, `#work/notebooks`, `#work/competitions` and `#work/benchmarks`; `#work` shows all work. Reload and browser history restore the category.

## Recommended implementation order

1. Separate general notebook execution/version publication from competition submission scoring. A notebook should be able to save a reproducible completed run even when its competition has no local answer key.
2. Unify resource visibility, sharing and ownership across datasets, model artifacts and notebook outputs, including private models and consistent group access.
3. Add scalable uploads and storage: resumable transfers, large model weights, paginated file trees, streaming previews, quotas and verified restore procedures.
4. Extend Your Work with shared resources/bookmarks and add organizer/moderation tools with audit history.

These are further implementation tasks, not features completed by this portal update. GPU support remains deferred as requested.

## Validation

The API regression suite passed 146 tests, including resource draft creation, immutable input selection, save/discard behavior and private dataset access. The web production build passed. Browser checks cover dataset/model detail routes, refresh and Back navigation, category-specific Your Work links, mobile overflow, first model-file upload, and automatic dataset attachment in a real notebook session.
